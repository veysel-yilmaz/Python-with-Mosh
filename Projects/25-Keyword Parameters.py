def greet_user(first_name, last_name):
    print(f'Hi {first_name} {last_name}!')
    print('Welcome aboard')
print("Start")
greet_user(last_name="Smith", first_name = "John") #keyword value, here the positiın doesnt matter
print("Finish") #it is better to write keywords for readibility
#takeaway is that use positional arguments(without first_name), for functions taking numerical valuew, use prefixes
when to use positional arguments first????
