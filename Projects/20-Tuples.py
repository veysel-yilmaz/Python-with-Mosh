# Tuples are immutable,
# unlike lists tuples cannot change items
# numbers = [1, 2, 3] this is a list
# numbers = (1, 2, 3) this is a tuple
numbers = (1, 2, 3)
#numbers. #there are only two basic methods available for tuples
# numbers[0] = 10 # will produce an error of tuples cannot be iterable
print(numbers[0])


