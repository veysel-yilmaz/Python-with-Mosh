#this shows how to create functions that return values
def square(number):
    #if we remove return here, python interpreter returns none by default.
    return number * number #by default, python returns none.None is an object. null in c, c++, java
result = square(3)
#name = input()
print(result)
#to make code shorter: print(square(3))
